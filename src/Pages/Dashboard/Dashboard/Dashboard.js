import React from 'react';
import SideBar from '../SideBar/SideBar';

const Dashboard = () => {
    return (
        <div>
            <h1>on way</h1>
            
        </div>
    );
};

export default Dashboard;